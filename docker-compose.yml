# Base: shared network only. Use compose overrides for services.
# Example: docker-compose -f docker-compose.yml -f compose/localstack-us-east-1.yml up -d

networks:
  localstack-network:
    driver: bridge
